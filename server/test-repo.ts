import { PrismaClient } from '@prisma/client'; const prisma = new PrismaClient(); async function main() { const data = { name: 'Cold Drink' }; const variants = [{ name: 'Regular', price: 100, isDefault: true, isAvailable: true }]; try { await prisma.$transaction(async (tx) => { await tx.menuItem.update({ where: { id: 14 }, data }); await tx.menuVariant.deleteMany({ where: { menuItemId: 14 } }); for (const v of variants) { await tx.menuVariant.create({ data: { menuItemId: 14, name: v.name, price: v.price, isDefault: v.isDefault, isAvailable: v.isAvailable, createdBy: 10, updatedBy: 10 } }); } }); console.log('Success'); } catch(e) { console.error('Error:', e); } } main().finally(() => prisma.$disconnect());