{
  "name": "restaurant-pos-monorepo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd server && npm install --include=dev && npx prisma generate && npm run build",
    "start": "cd server && npx prisma migrate deploy && npm run start"
  }
}
